首先举个例子
function () {
    alert("nice");
}
大家都知道这样声明一个匿名函数，并没有执行，如果不传递给变量，函数声明结束后将无法调用；
匿名函数可以传递给一个参数；
var x = function () {
    alert("nice");
}
需要执行函数是时入语句
x();
函数就会执行；
如果x替换为原函数声明；则如下所示
function () {
    alert("nice");
}();
按照道理来说等同于上面的x();这样就可以声明一个匿名函数并且立即执行。
但是经过测试，浏览器报错。

